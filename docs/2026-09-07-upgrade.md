# 写作管道与知识库改进记录（2026-09-07）

## 已落实

修复缺失风格文件、保存时漏传最终正文、收尾待办未完成、同主题共用工作记忆、同日文件覆盖、格式错误继续下传、任务恢复竞态。接入三个角色化 skill 适配版，保留来源指纹。

研究员新增查询规划、本地来源发现与原文抽取；审核收到作者材料和证据；润色后输出结构化论断表并核对原文片段。检索索引根据文件变更自动更新，状态与日期不再丢失。

保存与发布分离，最终稿必须人工确认；发布预览绑定正文指纹与目标，用户单独同意后才推送。发布失败可以重试，不夹带其他暂存内容或无关未推送提交。

知识库入库幂等并保留 arXiv 版本；分页未完成不推进水位，跨批去重，同日周报不覆盖已有复核记录；提供写作来源回库脚本。记忆服务离线不再中断周更的知识处理。

## 实测与限制

最终回归：写作仓库 33 项测试通过；知识库 38 项测试通过；现有知识库 lint 为 0 error、0 warning；两仓库 `git diff --check` 通过。写作依赖仍有一条 Pydantic `lifespan` 类型注解警告，未影响本次测试。并发任务心跳按任务分别保存，外部文件引用不再因显示相对路径而报错。

本地 mock 与临时 Git 远端用于流程、确认、隔离、重试验证；未调用真实写作模型，未往 GitHub 推送文章。实际模型仍可能错判证据关系，用户终审不可省略。

本地记忆 HTTP 初始化和新主题范围 memory_context 读取成功，返回 status=ok、无待复核项；没有在测试中向真实记忆库写入记录。旧 repo:writing 记忆没有迁移。

博客目标通过本地 Git remote 核实为 https://github.com/ac0033/ac0033.git，main 分支，articles 目录。本次没有修改该博客仓库。公开站点构建/部署不在当前 Markdown 仓库实现中。

本次没有调整模型档位、购买服务、注册任务或变更运行中的 MCP 连接。已有服务进程需重新加载代码后才能使用新增发布工具。人工确认按用户最新要求固定为“确认文章 → 保存本地 → 另行确认发布”。

## 后续评估方式

使用你确认的一个真实主题，记录搜证耗时、初稿和润色耗时、审核返工、引用是否准确支持论点、你实际修改了哪些段落。再决定是否缩减重复检索或调整模型预算，不以 mock 耗时冒充真实改善。

## 维护依据

- arXiv 分页参数： https://info.arxiv.org/help/api/user-manual.html
- Tavily search / extract：以本机安装 SDK 的公开参数为依据。
- skills 的原文来源与指纹：prompts/skills/sources.json。
- 自动测试：test_quality_pipeline.py、test_publishing.py、service 测试，以及知识库 tests/test_pipeline_integrity.py。
